<!--need to implement-->

<%=render :partial=>shared/dialog, :locals=>{:id=>element_id, :class=>"newCommentsDialog"} do%>

  <%remote_form_for 'email', :update=>"comments_block_#{listing.id}" do |f|%>
  <!-- need multiselect-->
    <%=f.label :content, 'Friend:'%><br/>
    <%= f.collection_select  :friend_id, current_user.friends, :id, :username, {:include_blank=>'Select'} %>
    <%=f.label :content, 'Email addresses:'%></br/>
    <%=f.text_area  :content, :cols => 18, :rows => 5%><br/>
    <%=f.img_submit "send_to_friend_submit_button", :onclick=>page.hide(element_id), :duration => 0.5 )-%>
  <%end%>

<%end%>
