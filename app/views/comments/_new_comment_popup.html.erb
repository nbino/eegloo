<%=render :partial=>shared/dialog, :locals=>{:id=>element_id, :class=>"newCommentsDialog"} do%>

  <%remote_form_for [listing, comment], :update=>"comments_block_#{listing.id}" do |f|%>
    <%=f.label :content, 'Comment'%><br/>
    <%=f.text_area  :content, :cols => 18, :rows => 5%><br/>
    <%=f.img_submit "add_comment_submit_button", :onclick=>page.hide(element_id), :duration => 0.5 )-%>
  <%end%>

<%end%>
